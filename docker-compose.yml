services:
  web:
    image: ghcr.io/hrohas-js/blumonlineeducationalplatformfront:latest
    container_name: blum-frontend
    restart: unless-stopped
    ports:
      - "8080:80"
    pull_policy: always
